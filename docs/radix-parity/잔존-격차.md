# 잔존 격차 5개 — Radix 1:1 parity

## 1. Collapsible — CSS variables
- [ ] `--radix-collapsible-content-height/width` — ResizeObserver + animation 일시 차단 + 측정
- [ ] mount animation prevention (requestAnimationFrame 1프레임)

이 부분은 ResizeObserver + 정밀 측정 패턴 필요. 2-3시간 분량.

## 2. Tabs — 세부 격차
- [x] orientation/dir 키보드 ✓
- [x] data-orientation ✓
- [x] asChild ✓
- [ ] **dir attribute 명시적 prop 전파** — Radix는 Root div에 dir prop을 그대로 전달. 우리는 useDirection hook만 사용.

거의 동등. 작은 격차만.

## 3. Form — Radix 비교
- 우리: native ValidityState 기반 (Phase 2 추가)
- Radix: 동일하지만 ServerValidationState 추가 지원 (`forceMatch` + `serverInvalid`)
- [ ] `serverInvalid` prop on Field
- [ ] `forceMatch` already 있음 ✓

## 4. RadioGroup — BubbleInput per Item
- [ ] 각 Radio에 hidden `<input type="radio" name={groupName}>` 동반 → form submission
- 작업량: 중간 (Switch/Checkbox BubbleInput 패턴 응용 가능)

## 5. Slot — Lazy component support
- [ ] React.use(promise) 기반 lazy children 지원
- 매우 드문 사용 케이스. 보류 적합.

---

## 종합

위 5개 잔존 격차는 모두 **사용 빈도 낮음 + 구현 복잡도 중~높음**. Radix 1:1 100% 동등을 위해 메우려면 4-6시간 추가 분량. 별도 마일스톤으로 처리 권장.
