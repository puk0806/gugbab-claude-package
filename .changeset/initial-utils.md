---
'@gugbab-ui/utils': minor
---

Initial release. Framework-agnostic pure utilities across 6 categories
(string, object, array, guard, fn, format) with zero runtime dependencies,
dual ESM/CJS build, and full TypeScript strict support.

Exports 22 functions: `isNil`, `isString`, `isNumber`, `isPlainObject`,
`isNonEmptyArray`, `capitalize`, `camelCase`, `kebabCase`, `truncate`,
`chunk`, `uniq`, `groupBy`, `pick`, `omit`, `deepMerge`, `sleep`, `once`,
`debounce`, `throttle`, `formatNumber`, `formatPercent`, `formatBytes`.
